<UserControl x:Class="VitaTest.Modules.Payments.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:VitaTest.Modules.Payments.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Платежи"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="17"
                       FontWeight="Bold"/>
        </Grid>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Payments}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="10"
                                Padding="10"
                                Background="#e4e4e4"
                                Margin="10">
                            <StackPanel>
                                <TextBlock Text="{Binding ID, StringFormat='Номер: {0}'}"
                                           FontWeight="Bold"
                                           FontSize="17"/>
                                <TextBlock Text="{Binding OrderId, StringFormat='Номер заказа: {0}'}"/>
                                <TextBlock Text="{Binding IncomeId, StringFormat='Номер поступления: {0}'}"/>
                                <TextBlock Text="{Binding PaymentAmount, StringFormat='Сумма платежа: {0}'}"/>
                                <TextBlock Text="{Binding CreatedAt, StringFormat='Создано: {0:dd.MM.yyyy}'}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
