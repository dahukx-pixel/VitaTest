<UserControl x:Class="VitaTest.Modules.Incomes.Views.IncomesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:VitaTest.Modules.Incomes.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Пополнения"
                       FontSize="17"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1">
            <Expander Header="Пополнить"
              Margin="10"
              HorizontalAlignment="Left"
              VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal"
                            Margin="10">
                    <Button Content="Добавить"
                            Margin="0, 0, 10, 0"
                            Command="{Binding AddIncomeCommand}"/>
                    <TextBox materialDesign:HintAssist.Hint="Сумма пополнения (р)"
                             Text="{Binding IncomeSum}"/>
                </StackPanel>
            </Expander>
        </Grid>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Incomes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="10"
                                Padding="10"
                                Background="#e4e4e4"
                                Margin="10">
                            <StackPanel>
                                <TextBlock Text="{Binding ID, StringFormat='Номер: {0}'}"
                                           FontWeight="Bold"
                                           FontSize="17"/>
                                <TextBlock Text="{Binding Amount, StringFormat='Сумма пополнения: {0}'}"/>
                                <TextBlock Text="{Binding Balance, StringFormat='Остаток: {0}'}"/>
                                <TextBlock Text="{Binding CreatedAt, StringFormat='Создано: {0:dd.MM.yyyy}'}"/>
                                <TextBlock Text="{Binding UpdatedAt, StringFormat='Обновлено: {0:dd.MM.yyyy}'}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
