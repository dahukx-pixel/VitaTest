<UserControl x:Class="VitaTest.Modules.Settings.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:VitaTest.Modules.Settings.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0">
            <DockPanel LastChildFill="False">

                <DockPanel.Resources>
                    <Style TargetType="TextBox"
                           BasedOn="{StaticResource MaterialDesignFilledTextBox}">
                        <Setter Property="Margin" Value="10"/>
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="MinWidth" Value="300"/>
                        <Setter Property="FontSize" Value="17"/>
                    </Style>
                    <Style TargetType="PasswordBox"
                           BasedOn="{StaticResource MaterialDesignFilledPasswordBox}">
                        <Setter Property="Margin" Value="10"/>
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="MinWidth" Value="300"/>
                        <Setter Property="FontSize" Value="17"/>
                    </Style>
                </DockPanel.Resources>

                <Border Padding="10"
                        Margin="10"
                        CornerRadius="10"
                        DockPanel.Dock="Top">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0.2*"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <TextBlock Text="Настройки базы данных"
                               HorizontalAlignment="Center"
                               Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                        </Grid>

                        <Grid Grid.Row="1">
                            <StackPanel>
                                <TextBox materialDesign:HintAssist.Hint="Адрес БД"
                                         Text="{Binding DbAddress}"/>
                                <TextBox materialDesign:HintAssist.Hint="Название БД"
                                         Text="{Binding DbName}"/>
                                <TextBox materialDesign:HintAssist.Hint="Пользователь"
                                         Text="{Binding DbLogin}"/>
                                <PasswordBox x:Name="PasswordBox"
                                             materialDesign:HintAssist.Hint="Пароль"/>
                                <CheckBox Content="Локальная БД"
                                          IsChecked="{Binding LocalDb}"
                                          FontSize="17"
                                          Margin="10"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </DockPanel>
        </ScrollViewer>

        <Grid Grid.Row="1">
            <Button Content="Сохранить"
                    Width="100"
                    Foreground="White"
                    HorizontalAlignment="Center"
                    Click="OnSaveButtonClick"/>
        </Grid>
    </Grid>
</UserControl>
